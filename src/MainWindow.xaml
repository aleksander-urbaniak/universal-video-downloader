<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Universal Video Downloader" Height="650" Width="750"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgBrush}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush">#F3F4F6</SolidColorBrush>
        <SolidColorBrush x:Key="CardBrush">#FFFFFF</SolidColorBrush>
        <SolidColorBrush x:Key="TextBrush">#1F2937</SolidColorBrush>
        <SolidColorBrush x:Key="SubTextBrush">#6B7280</SolidColorBrush>
        <SolidColorBrush x:Key="BorderBrush">#E5E7EB</SolidColorBrush>
        <SolidColorBrush x:Key="InputBgBrush">#FFFFFF</SolidColorBrush>
        <SolidColorBrush x:Key="PrimaryBrush">#4F46E5</SolidColorBrush>
        <SolidColorBrush x:Key="DestructiveBrush">#DC2626</SolidColorBrush>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="{DynamicResource InputBgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="{DynamicResource InputBgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <Grid>
                                <ToggleButton Name="ToggleButton" 
                                              Template="{DynamicResource ComboBoxToggleButton}" 
                                              Grid.Column="2" 
                                              Focusable="false"
                                              ClickMode="Press" 
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <ContentPresenter Name="ContentSite"
                                                  IsHitTestVisible="False" 
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="10,0,23,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left" />
                                <Popup Name="Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True" 
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Grid Name="DropDown"
                                          SnapsToDevicePixels="True"
                                          MinWidth="{TemplateBinding ActualWidth}"
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder"
                                                Background="{DynamicResource InputBgBrush}"
                                                BorderThickness="1"
                                                BorderBrush="{DynamicResource BorderBrush}"/>
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" 
                        Grid.ColumnSpan="2"
                        CornerRadius="6"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0" />
                <Path x:Name="Arrow"
                      Grid.Column="1"     
                      Fill="{DynamicResource SubTextBrush}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
        </ControlTemplate>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource InputBgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        </Style>

        <Style x:Key="DestructiveButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource DestructiveBrush}"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource CardBrush}" Padding="20,15"
                BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderBrush}">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Border Background="{DynamicResource PrimaryBrush}" CornerRadius="6" Width="30" Height="30" Margin="0,0,10,0">
                        <TextBlock Text="V" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Universal Video Downloader" FontWeight="Bold" FontSize="18"
                               Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Name="ThemeToggleBtn" Content="Dark Mode" HorizontalAlignment="Right"
                        Style="{StaticResource SecondaryButton}" FontSize="12" Height="20" Width="70"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Margin="30,30,30,10">

            <TextBlock Text="Paste Link (TikTok, YouTube, Instagram, Facebook or Direct URL)"
                       Foreground="{DynamicResource SubTextBrush}" Margin="0,0,0,8" FontWeight="SemiBold"/>
            
            <Grid Height="40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Name="InputUrl" FontSize="14" VerticalContentAlignment="Center"/>

                <ComboBox Grid.Column="1" Name="ComboResolution" Width="100" VerticalContentAlignment="Center"
                          FontSize="13" SelectedIndex="0" Visibility="Collapsed" Margin="10,0,0,0"
                          Background="{DynamicResource InputBgBrush}" Foreground="{DynamicResource TextBrush}">
                    <ComboBoxItem Content="Highest Available"/>
                    <ComboBoxItem Content="2160p (4K)"/>
                    <ComboBoxItem Content="1440p (2K)"/>
                    <ComboBoxItem Content="1080p (FHD)"/>
                    <ComboBoxItem Content="720p (HD)"/>
                    <ComboBoxItem Content="480p"/>
                    <ComboBoxItem Content="360p"/>
                </ComboBox>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                <CheckBox Name="ChkAudioOnly" Content="Audio Only" FontSize="14" VerticalAlignment="Center"
                          Foreground="{DynamicResource TextBrush}" Cursor="Hand" Margin="0,0,15,0"/>

                <ComboBox Name="ComboAudioFormat" Width="90" Height="30" VerticalContentAlignment="Center"
                          FontSize="13" SelectedIndex="0" IsEnabled="False"
                          Background="{DynamicResource InputBgBrush}" Foreground="{DynamicResource TextBrush}">
                    <ComboBoxItem Content="Available"/>
                    <ComboBoxItem Content="mp3"/>
                    <ComboBoxItem Content="m4a"/>
                    <ComboBoxItem Content="wav"/>
                </ComboBox>
            </StackPanel>

            <TextBlock Text="Save Location" Foreground="{DynamicResource SubTextBrush}"
                       Margin="0,20,0,8" FontWeight="SemiBold"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Name="InputPath" IsReadOnly="True" FontSize="14" Height="40"/>
                <Button Grid.Column="2" Name="BtnBrowse" Content="Browse..." Style="{StaticResource SecondaryButton}" Height="20" Width="60"/>
            </Grid>

            <Grid Margin="0,30,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Name="BtnDownload" Content="Download" FontSize="16" FontWeight="Bold" Height="50"/>
                <Button Grid.Column="2" Name="BtnStop" Content="Stop" Style="{StaticResource DestructiveButton}" FontSize="16" FontWeight="Bold" Height="50" IsEnabled="False"/>
            </Grid>

        </StackPanel>

        <Border Grid.Row="2" Margin="30,10,30,30" Background="{DynamicResource CardBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="10,5" Background="{DynamicResource BgBrush}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Activity Log" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource SubTextBrush}" VerticalAlignment="Center"/>

                        <Button Grid.Column="1" Name="BtnCopyLog" Content="Copy" Style="{StaticResource SecondaryButton}" FontSize="12" Padding="10,4" Width="50" Height="20"/>
                        <Button Grid.Column="3" Name="BtnClearLog" Content="Clear" Style="{StaticResource SecondaryButton}" FontSize="12" Padding="10,4" Width="50" Height="20"/>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1" Name="LogScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="5">
                    <StackPanel Name="LogPanel" Orientation="Vertical"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Row="3" Padding="15" Background="{DynamicResource CardBrush}"
                BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Grid Margin="0,0,0,5">
                         <TextBlock Name="TxtStatus" Text="Ready" Foreground="{DynamicResource SubTextBrush}" FontSize="13" HorizontalAlignment="Left"/>
                         <TextBlock Name="TxtProgressDetails" Text="" Foreground="{DynamicResource SubTextBrush}" FontSize="12" HorizontalAlignment="Right"/>
                    </Grid>
                    <ProgressBar Name="PrgBar" Height="8" BorderThickness="0" Background="{DynamicResource BgBrush}" Foreground="{DynamicResource PrimaryBrush}" Minimum="0" Maximum="100"/>
                </StackPanel>

                <TextBlock Grid.Column="1" Name="TxtVersion" Text="{x:Static sys:String.Empty}" Foreground="{DynamicResource SubTextBrush}" FontSize="13" VerticalAlignment="Bottom" Opacity="0.5"/>
            </Grid>
        </Border>

    </Grid>
</Window>